# ===== GitLab (локальный) =====
GITLAB_HOSTNAME=gitlab.local
GITLAB_HTTP_PORT=8080
# Оставьте HTTPS закомментированным для лёгкой конфигурации
# GITLAB_HTTPS_PORT=8443
GITLAB_SSH_PORT=2222

# Пароль root будет задан только при самом первом запуске контейнера
GITLAB_ROOT_PASSWORD=ChangeMe123!

# Токен для регистрации раннеров (необязательно, можно оставить пустым)
GITLAB_RUNNERS_TOKEN=

# Подсказка:
# Добавьте в /etc/hosts строку:
# 127.0.0.1 gitlab.local
# или используйте http://localhost:8080